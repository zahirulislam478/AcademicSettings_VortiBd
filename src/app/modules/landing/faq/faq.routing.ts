import { Route } from '@angular/router';
import { LandingFaqComponent } from './faq.component';

export const landingFaqRoutes: Route[] = [
    {
        path     : '',
        component: LandingFaqComponent
    }
];
