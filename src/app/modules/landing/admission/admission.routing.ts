import { Route } from '@angular/router';
import { LandingAdmissionComponent } from './admission.component';

export const landingAdmissionRoutes: Route[] = [
    {
        path     : '',
        component: LandingAdmissionComponent
    }
];
