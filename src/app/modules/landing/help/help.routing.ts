import { Route } from '@angular/router';
import { LandingHelpComponent } from './help.component';

export const landingHelpRoutes: Route[] = [
    {
        path     : '',
        component: LandingHelpComponent
    }
];
